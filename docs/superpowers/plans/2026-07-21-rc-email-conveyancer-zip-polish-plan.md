# RC Email, Conveyancer, and ZIP Polish Implementation Plan

> **For Codex:** Execute this plan test-first in the existing isolated `fix/staff-dropdown-seeding-v2` worktree. Keep the scope limited to the five approved RC-polish contracts.

**Goal:** Add the exact prepared-email contract, editable conveyancer combobox, client-aware ZIP entry names, and standard date-first ZIP package name without changing storage, workflow, PDF layout, or application version.

**Architecture:** Keep the existing single-file runtime architecture. Add narrow formatting and filename helpers in `js/app.js`, retain existing event and persistence paths, render `#iaSolicitor` as an input/datalist at the current configurable-control boundary, and update only the service-worker cache constant required for runtime invalidation.

**Tech Stack:** Vanilla JavaScript, HTML generated by `js/app.js`, JSZip, Playwright-based Node tests, Node assertion tests.

## Task 1: Characterize the missing RC contracts

**Files:**
- Create: `tests/rc-email-conveyancer-zip-polish.test.mjs`
- Modify only where obsolete assumptions require it: existing IA/mobile/service-worker tests

1. Add failing focused assertions for the exact subject and approved multiline email body, including recipient/CC deduplication and one captured contract timestamp.
2. Add failing assertions for in-person and Zoom next-appointment selection, time formatting, and complete block omission when blank.
3. Add failing assertions that `#iaSolicitor` is an accessible input/list combobox, accepts configured and arbitrary values, and preserves custom values through draft reload and IA PDF generation.
4. Add failing assertions for actual client names in ZIP entries, fallback names, shared sanitisation, and the exact date-first ZIP package filename.
5. Run the focused test and confirm failures correspond to the legacy implementation rather than test setup.

## Task 2: Implement prepared-email formatting

**Files:**
- Modify: `js/app.js`

1. Add narrow local date and 12-hour time formatters without changing existing PDF/date helpers.
2. Resolve the relevant next appointment from existing fields only: EOI date/time for in-person and Client Review date for Zoom.
3. Capture one `Date` when email content is built and reuse its formatted value.
4. Replace the legacy subject/body with the exact approved subject and structured body; omit the complete Next Appointment block when unavailable.
5. Preserve recipient/CC resolution, deduplication, native share, timeout, and fallback behavior.
6. Run focused email tests.

## Task 3: Implement the editable conveyancer combobox

**Files:**
- Modify: `js/app.js`
- Modify: affected tests that assume a native select

1. Render `#iaSolicitor` as a labelled native text input with `list` referencing a configured datalist.
2. Keep `B.O.S.S Conveyancing` as the fresh default and preserve configured option order.
3. Reuse existing input binding, draft capture/restoration, invalidation, and `fieldText('iaSolicitor')` PDF consumption.
4. Verify configured selection, arbitrary text, draft round-trip, keyboard accessibility, and PDF output value.
5. Run focused and IA PDF tests.

## Task 4: Implement shared filename sanitisation and ZIP naming

**Files:**
- Modify: `js/app.js`

1. Harden the existing filename-part helper as the single path for Windows-invalid characters, whitespace collapse, edge trimming, trailing periods, readable fallbacks, and length safety.
2. Resolve Client 1 and Client 2 names for ID entries and additional-document aliases, using literal fallbacks only when the relevant name is blank.
3. Standardise both appointment modes on `DD-MM-YYYY - <Client Names> - Sales Appointment Documents.zip`.
4. Preserve unrelated generated filenames and ZIP contents.
5. Run focused ZIP and IA/PDF smoke tests.

## Task 5: Cache invalidation and regression verification

**Files:**
- Modify: `service-worker.js`
- Modify: focused service-worker expectations if required

1. Advance only `CACHE_VERSION` from `v2.7.0-alpha.9` to `v2.7.0-alpha.10`; keep application version `2.7.0-alpha.1` and caching strategy unchanged.
2. Run syntax checks, `git diff --check`, focused RC tests, service-worker tests, IA/PDF smoke, and the authoritative Phase 5 regression suite.
3. Confirm no storage keys/shapes, IDs, PDF layout, validation, workflow, or application version changed.
4. Inspect the final diff for unrelated changes and confirm the worktree contains no generated evidence/artifacts.

## Task 6: Focused local commit

**Files:**
- Include: approved specification, this plan, runtime changes, and tests

1. Stage only the approved files.
2. Create one focused local commit.
3. Report the commit hash, exact generated-email example, representative ZIP listing, verification results, and any residual risks.

## Self-review

- Scope maps one-to-one to the five approved requirements; no architecture, permissions, storage, workflow, or PDF-layout work is included.
- The latest approved email template in the task request overrides the abbreviated body example in the earlier design document.
- All next-appointment data comes from existing fields; no time is invented for Zoom.
- Test-first order proves the current gaps before runtime edits.
- Cache and application versions follow the explicit release contract.
- The single-commit boundary includes the previously uncommitted approved specification.
